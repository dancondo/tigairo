I"7(function() {
  var aaa, imagePreview, input;

  imagePreview = function() {
    var filerd;
    console.log(input);
    if (input.files && input.files[0]) {
      filerd = new FileReader();
      console.log(filerd);
      filerd.onload = function(event) {
        return $('imgpreview').attr('src', e.target.result);
      };
      return filerd.readAsDataURL(input.files[0]);
    }
  };

  input = $('.file-input').onload;

  console.log(input);

  aaa = $('.form-control .string .optional');

  console.log(aaa);

  aaa.onload = console.log(aaa);

}).call(this);
:ET