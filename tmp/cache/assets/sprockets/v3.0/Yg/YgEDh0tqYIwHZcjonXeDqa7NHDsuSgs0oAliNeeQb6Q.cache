I"(function() {
  var fileInput, imagePreview;

  imagePreview = function(input) {
    var filerd;
    console.log(input);
    if (input.files && input.files[0]) {
      filerd = new FileReader();
      console.log(filerd);
      filerd.onload = function(event) {
        return $('imgpreview').attr('src', e.target.result);
      };
      return filerd.readAsDataURL(input.files[0]);
    }
  };

  fileInput = $('.file-input');

  console.log(file - input);

  fileInput.onchage = imagePreview(this);

}).call(this);
:ET