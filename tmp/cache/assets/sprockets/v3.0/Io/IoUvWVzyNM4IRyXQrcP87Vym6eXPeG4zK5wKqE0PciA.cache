I"c(function() {
  var fileInput, imagePreview;

  imagePreview = function(input) {
    var filerd, image;
    console.log(input);
    if (input.files && input.files[0]) {
      filerd = new FileReader();
      console.log(filerd);
      image = $('#imgpreview');
      filerd.onload = function(event) {
        image.css('display', 'block', 'position', 'absolute', 'top', 0);
        return image.attr('src', event.target.result);
      };
      return filerd.readAsDataURL(input.files[0]);
    }
  };

  fileInput = $('.file-input').on("change", function() {
    return imagePreview(this);
  });

}).call(this);
:ET