I" (function() {
  var toggleBar;

  toggleBar = function(trigger) {
    var navbar;
    navbar = $($(trigger).data("target"));
    navbar.toggle('slide', {
      direction: 'right'
    });
    return $('body').classToggle('no-flow');
  };

  $('#navbar-toggle').on("click", function() {
    return toggleBar(this);
  });

  $('#navbar-close').on("click", function() {
    return toggleBar(this);
  });

}).call(this);
:ET